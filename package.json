{
  "name": "cinaro",
  "version": "1.0.0",
  "private": true,
  "description": "CINARO Arabic movies and series progressive web app",
  "type": "module",
  "scripts": {
    "dev": "node scripts/serve.mjs",
    "test": "node --check web/data.js && node --check web/app.js && node --check web/sw.js && node scripts/validate.mjs"
  },
  "engines": {
    "node": ">=20"
  }
}
